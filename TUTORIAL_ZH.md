# manuscript-revision 技能完整教程

本教程将带您完整走过 `manuscript-revision` 技能的每一个阶段，从安装到生成一份经过严格修订和人性化处理的手稿。假设您已安装 Claude Code，并对命令行有基本了解。

---

## 目录

1. [技能功能概述](#1-技能功能概述)
2. [安装](#2-安装)
3. [快速入门——第一次修订](#3-快速入门第一次修订)
4. [第一轮：导入与诊断](#4-第一轮导入与诊断)
5. [第二轮：科学严谨性改写](#5-第二轮科学严谨性改写)
6. [第三轮：人性化处理](#6-第三轮人性化处理)
7. [验证与完成](#7-验证与完成)
8. [使用人性化工具](#8-使用人性化工具)
9. [独立使用还是组合使用？](#9-独立使用还是组合使用)
10. [常见问题排查](#10-常见问题排查)

---

## 1. 技能功能概述

`manuscript-revision` 技能将您已经写好的手稿——教材、论文、技术报告或任何长篇文档——通过一个结构化的三轮流水线进行改写：

```
第一轮：导入与诊断
  ↓  读取现有手稿 → 清点章节 → 评分五个严谨性维度的缺口

第二轮：科学严谨性改写
  ↓  分类论断 → 匹配证据 → 形式化论证 → 标记缺失引用 → 规范符号

第三轮：人性化处理
  ↓  调用 blader/humanizer（英文）或 op7418/Humanizer-zh（中文）
     → 或 Claude 原生回退 → 检查严谨性回退 → 定稿

验证
  ↓  运行 validate_revision.py → 确认所有关卡通过 → 标记完成
```

**忠实性约束**是本技能区别于"重写"的核心：每一项改动都必须保留作者的原始论点。如果严谨性修复需要改变论点，技能会将其标记为冲突并提交给您裁决，而不是私自解决。

---

## 2. 安装

### Claude Code

将技能文件夹复制到您的 Claude Code 技能目录：

```bash
cp -r skills/manuscript-revision ~/.claude/skills/
```

重启 Claude Code。当您描述修订任务时，技能会自动触发。

### Claude.ai

1. 将 `skills/manuscript-revision/` 文件夹（注意：不是整个仓库）压缩为 zip。
2. 进入**设置 → 功能 → 技能 → 上传技能**。
3. 上传 zip 文件并开启技能。

### 可选：安装人性化工具

英文人性化：
```bash
pip install humanizer   # 或参考 https://github.com/blader/humanizer
```

中文人性化：
```bash
git clone https://github.com/op7418/Humanizer-zh
cd Humanizer-zh && pip install -r requirements.txt
```

即使不安装这两个工具，技能也能通过内置的 Claude 原生人性化处理正常工作——这两个工具只是增加了额外的后处理层。

---

## 3. 快速入门——第一次修订

假设您在 `~/my-book/` 目录下有一份 Markdown 格式的教材草稿。完整流程如下：

**第一步：** 告诉 Claude 您想做什么。

```
我在 ~/my-book/ 有一份书稿，想让它更有科学严谨性，然后对结果进行人性化处理。
请使用 manuscript-revision 技能。
```

**第二步：** Claude 检测阶段（导入），读取 `references/ingest-and-diagnose.md`，然后运行诊断脚本：

```bash
python3 scripts/diagnose.py ~/my-book/ --output ~/my-book-revision/
```

这会在您的项目目录中生成 `MANUSCRIPT_ANALYSIS.md`——一份逐章节的缺口分析框架。

**第三步：** 审阅并批准诊断结果。

Claude 会呈现如下摘要：

```
文档：~/my-book/
章节数：42 | 词数：约 65,000
语言：中文

优先处理章节（2个以上维度缺失）：
  CH03-S02 — 缺失：证据、来源完整性
  CH07-S01 — 缺失：论断分类、形式化论证
  CH11-S04 — 缺失：证据、符号规范

忠实性锚点：
  CH01："本书主张，系统设计最好通过形式化容量模型来理解，
         而不是凭借经验启发式方法。"
  ...

是否批准此分析并开始制定修订计划？
```

请仔细检查忠实性锚点——这些是技能在未经您明确许可的情况下不会更改的内容。

**第四步：** 批准后，Claude 会生成覆盖每个章节的 `REVISION_PLAN.md`。

**第五步：** 运行第二轮（严谨性改写）和第三轮（人性化处理）——Claude 逐章节处理并记录每一项改动。

**第六步：** 验证。

```bash
python3 scripts/validate_revision.py ~/my-book-revision/
```

---

## 4. 第一轮：导入与诊断

### 支持的输入格式

| 格式 | 准备方式 |
|---|---|
| Markdown (.md) | 无需处理，直接读取 |
| 纯文本 (.txt) | 无需处理，直接读取 |
| LaTeX (.tex) | 无需处理，以 `\section` 为章节边界 |
| DOCX | 先转换：`pandoc input.docx -o output.md` |
| PDF | 从 PDF 阅读器导出为文本，或：`pdftotext input.pdf output.txt` |
| Jupyter Notebook | 直接读取——Markdown 单元视为正文，代码单元视为证据 |

### 五个严谨性维度

诊断会在这五个维度上对每个章节打分：

| 维度 | 检查内容 |
|---|---|
| **论断分类** | 论断是否有标签？（已确立结论 / 实证论断 / 解读 / 假说） |
| **证据匹配** | 所提供的证据是否与论断的强度相符？ |
| **形式化论证** | 术语是否在使用前有定义？核心结论是否经过推导而不是直接断言？ |
| **来源完整性** | 需要引用的地方是否有引用？引用是否真实且范围合适？ |
| **符号规范** | 符号是否在使用前有说明？是否保持一致使用？ |

每个维度的评分为**充分**、**部分**或**缺失**三个等级。

### 忠实性锚点

对于每个章节，Claude 仅使用文本中出现的语言提取作者的核心论点。这成为所有改动的不可逾越的约束。示例：

```
CH04 忠实性锚点：
  "所提出的调度算法通过限制队列深度来降低尾部延迟，
   而不是优化平均吞吐量。"
```

任何改动都不允许改变这个论点，即使审稿人可能会用不同方式表述它。

### 批准诊断结果

当 Claude 呈现 `MANUSCRIPT_ANALYSIS.md` 时，请检查：

- 忠实性锚点是否准确？如果某个锚点误解了您的论点，请在批准前纠正它。
- 是否有章节标记为 `UNREADABLE`（无法读取）？这些章节需要格式转换后才能进行改写。
- 章节 ID 是否稳定？它们将用于修订日志，后期更改会带来麻烦。

**在批准分析结果之前，请不要进入第二轮。** 这是一个检查点，不是走过场。

---

## 5. 第二轮：科学严谨性改写

### 会改什么，不会改什么

严谨性改写有五类改动，全部可追溯：

**论断分类** — 为不同类型的论断添加标签：

```
改写前：
  "缓存无关算法在现代硬件上优于缓存感知算法。"

改写后：
  "缓存无关算法在现代硬件上优于缓存感知算法
   [实证论断——引用：Frigo et al. 1999，或需要引用]。"
```

**证据匹配** — 调整表述以匹配证据强度：

```
改写前（论断强度超出证据支撑）：
  "这证明了该方法是最优的。"

改写后（论断弱化以匹配单次实验）：
  "该实验表明，该方法在测试工作负载上与基准方法具有竞争力
   [说明性案例；推广需要更广泛的评估]。"
```

**形式化论证** — 添加定义和推导概述：

```
改写前：
  "设 T 为系统的吞吐量。"

改写后：
  "设 T 表示系统在声明负载模型下的稳态吞吐量，
   单位为每秒请求数。"
```

**来源完整性** — 插入占位符，从不凭空捏造引用：

```
改写前：
  "研究表明，70% 的分布式系统故障由配置错误引起。"

改写后：
  "研究表明，70% 的分布式系统故障由配置错误引起
   [需要引用：关于生产事故的实证研究]。"
```

**符号规范** — 标准化并引入符号：

```
改写前：
  "μ 可由 N 和 λ 计算得出。"

改写后：
  "服务率 μ（请求/秒）可由服务器数量 N 和到达率 λ（请求/秒）计算得出。"
```

### REVISION_LOG.md

每一项改动都有记录：

```
| 日期       | 章节 ID    | 关卡            | 操作                                 | 忠实性   | 状态     |
| 2026-07-26 | CH03-S02   | 论断分类        | 在第 4 段添加了 [假说] 标记           | 已保留   | 完成     |
| 2026-07-26 | CH07-S01   | 证据            | 将"证明"弱化为"表明"                 | 已保留   | 完成     |
| 2026-07-26 | CH11-S04   | 忠实性冲突      | 作者声称算法是"最优的"；无证明或引用  | 冲突     | 待解决   |
```

### 忠实性冲突

当严谨性修复需要改变您的论点时，技能会停下来标记冲突，而不是替您做决定：

```
[忠实性冲突 FC-003]
章节：CH11-S04
原始论断："该算法可证明是最优的。"
严谨性问题：没有提供证明或引用。该论断无法在没有证据的情况下保留。
选项：
  A. 提供证明或引用（作者操作）。
  B. 弱化为"我们推测该算法是最优的"[假说]。
  C. 删除最优性声明。
等待作者决定。
```

在冲突解决之前，该章节不能标记为完成。验证器会强制执行这一规则。

---

## 6. 第三轮：人性化处理

### 人性化处理解决的问题

AI 生成和大量编辑的文本通常有可识别的模式：

- 句子长度重复（所有句子约 20-25 字）
- 叠加式模糊语气："或许可以认为这可能……"
- 填充性过渡词："此外，""值得注意的是，""不仅如此，"
- 为变化而变化的近义词替换：同一概念交替用"方法"、"途径"、"策略"、"手段"
- 对于目标读者而言过于正式的文风

人性化处理消除这些模式，同时不削弱第二轮建立的科学精确性。

### 工具路由

```
手稿语言  →  使用工具
──────────────────────────────────────
英文      →  blader/humanizer（若已安装）→ Claude 原生回退
中文      →  op7418/Humanizer-zh（若已安装）→ Claude 原生回退
混合      →  按语言路由每个章节
```

### 人性化处理不会做的事

- 不移除论断分类标记
- 不软化假说或解读标签
- 不删除已声明的假设或适用域说明
- 不移除 `[需要引用]` 占位符
- 不改变已定义的符号

如果人性化后的段落削弱了上述任何内容，技能会恢复精确表述并在日志中记录。

### 示例

```
处理前（典型 AI 风格）：
  "此外，值得注意的是，所提出的方法论在受控实验环境中
   与已建立的基准方法相比，展现出了卓越的性能特征。"

处理后（人性化后，严谨性保留）：
  "该方法在受控实验中优于现有基准方法
   [说明性案例；见表 3]。"
```

---

## 7. 验证与完成

在每轮结束时（不仅仅是最后）运行验证器：

```bash
python3 scripts/validate_revision.py <项目目录>
```

所有关卡通过时的示例输出：

```
Manuscript Revision Validator — /Users/you/my-book-revision

  ✓ [PASS]  analysis-approved    MANUSCRIPT_ANALYSIS.md 已有作者批准标记
  ✓ [PASS]  plan-faithfulness    忠实性约束已声明
  ✓ [PASS]  plan-humanizer       人性化工具配置已存在
  ✓ [PASS]  plan-approved        REVISION_PLAN.md 已有作者批准标记
  ✓ [PASS]  log-has-entries      找到 47 条已完成的日志条目
  ✓ [PASS]  no-open-conflicts    无未解决的忠实性冲突
  ✓ [PASS]  progress-populated   PROGRESS.md 内容完整

  7/7 个关卡通过。
  结构验证完成。这只确认了框架的完整性。
  科学准确性和作者批准是独立的要求。
```

验证器确认**结构完整性**——即每个章节都已处理并记录在案。它不验证科学内容是否正确或作者是否满意。这些是您的责任。

---

## 8. 使用人性化工具

### blader/humanizer（英文）

安装：
```bash
# 请参考仓库获取当前安装方式
pip install humanizer
```

技能的调用方式：
```bash
humanizer --input section.md --output section-humanized.md
```

处理后，Claude 会对比原文和人性化后的输出，在接受之前检查是否有严谨性回退。

### op7418/Humanizer-zh（中文）

安装：
```bash
git clone https://github.com/op7418/Humanizer-zh
cd Humanizer-zh && pip install -r requirements.txt
```

调用方式：
```bash
python3 humanizer.py --input section.md --output section-humanized.md
```

### Claude 原生回退

如果两个工具都没有安装，Claude 会直接根据 `references/humanize-pass.md` 中的原则进行人性化处理。效果略逊于专用工具，但对于消除常见 AI 文风模式仍然有效。日志中会记录 `tool: claude-native`，便于您了解使用了哪种方式。

---

## 9. 独立使用还是组合使用？

### 作为独立技能

**是的——本技能完全自包含。** 除了 Python 3（用于脚本）和 Claude 外，没有硬性依赖。您可以在不安装任何人性化工具或其他技能的情况下修订任何手稿。

典型的独立工作流：

```
1. 将手稿文件放入一个文件夹。
2. 调用："请修订 ~/my-book/ 的手稿，提升科学严谨性。"
3. 批准诊断结果和修订计划。
4. 观察每个章节的改写和人性化处理。
5. 运行验证器。
6. 审阅修订后的输出。
```

### 与其他技能组合使用

**与 `scientific-textbook` 技能（互补组合）**

这两个技能构成了一个完整的写作系统：

| 场景 | 使用技能 |
|---|---|
| 从零开始写一本新书 | `scientific-textbook` |
| 改进您已有的草稿 | `manuscript-revision` |
| 用 `scientific-textbook` 写了一本书，现在想做最终润色 | `manuscript-revision`（仅人性化轮） |
| 有现成的书，想将其重构为系统性课程 | 两者结合：`manuscript-revision` 做严谨性处理，然后用 `scientific-textbook` 做课程设计 |

**与网络搜索 / 来源检索技能**

严谨性改写会插入 `[需要引用：<类型>]` 占位符，但不会搜索来源。结合网络搜索技能或来源检索工作流，可以在同一会话中填充这些占位符。

**文档转换（Pandoc）**

对于 DOCX 和 PDF 输入，在调用本技能之前先运行 Pandoc：

```bash
pandoc manuscript.docx -o manuscript.md --wrap=none
```

这不是技能的依赖项——只是一行预处理命令。但与文档处理技能或 MCP 服务器配合使用，可以让转换过程更加顺畅。

### 什么时候不应该组合

不要在同一个项目上同时运行 `scientific-textbook` 和 `manuscript-revision`。它们的权限顺序和框架结构不同，会产生冲突。每次只使用其中一个；有意识地按顺序使用。

---

## 10. 常见问题排查

**技能没有自动触发。**

添加更明确的触发短语："使用 manuscript-revision 技能修订我的书。"如果技能安装正确，这总能加载它。

**`diagnose.py` 产生了太多章节。**

这发生在手稿有很多低级标题的情况下。在批准分析结果之前，手动在 `MANUSCRIPT_ANALYSIS.md` 中合并相关行。

**忠实性冲突阻碍了进度。**

阅读 `REVISION_LOG.md` 中的冲突条目。从列出的选项中选择一个（提供证据、弱化论断或删除），并将您的决定告诉 Claude。它会应用所选的解决方案并将冲突状态更新为 `resolved`（已解决）。

**即使我已经批准，验证器仍然报告 `analysis-approved: FAIL`。**

验证器在 `MANUSCRIPT_ANALYSIS.md` 中查找 `- [x]` 或 `[x] Author`。请确保您或 Claude 更新了复选框：将文件中的 `- [ ] Author has reviewed` 改为 `- [x] Author has reviewed`。

**找不到人性化工具。**

技能会自动回退到 Claude 原生人性化处理。如果您想使用工具，先安装它（见第 8 节），然后告诉 Claude："使用 blader/humanizer 重新运行人性化处理。"

**修订后的输出改变了我不想改变的内容。**

查看 `REVISION_LOG.md` 中对应章节的记录。每一项改动都有条目。如果某项改动有误，告诉 Claude："撤销 CH03-S02 的论断分类改写"——它会撤销该特定条目并记录撤销操作。
